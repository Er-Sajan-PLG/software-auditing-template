// Loaded straight from source on purpose — this is the demo's planted defect.
const config = {
  api_key: 'example_key_not_real_12345',
  dbPassword: 'example_db_password_99',
  endpoint: 'http://api.internal.example.com/v1/orders',
};

module.exports = config;
