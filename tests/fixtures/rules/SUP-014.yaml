rule: SUP-014
negative:
  files:
    .github/workflows/release.yml: |
      name: Release
      jobs:
        sbom:
          steps:
            - run: syft packages dir:. -o spdx-json
positive:
  files:
    .github/workflows/release.yml: |
      name: Release
      jobs:
        build:
          steps:
            - run: echo ok
  expect: MISSING
