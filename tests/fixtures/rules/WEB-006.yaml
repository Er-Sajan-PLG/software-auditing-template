rule: WEB-006
negative:
  files:
    src/ErrorBoundary.tsx: |
      class ErrorBoundary extends React.Component {
        componentDidCatch(err) { report(err); }
      }
positive:
  files:
    src/index.ts: |
      export const x = 1;
  expect: MISSING
