rule: CTNR-006
negative:
  files:
    Dockerfile: |
      FROM node:20-slim
      HEALTHCHECK CMD curl -f http://localhost/ || exit 1
      CMD ["node", "index.js"]
positive:
  files:
    Dockerfile: |
      FROM node:20-slim
      CMD ["node", "index.js"]
  expect: MISSING
