rule: SOLID-006
negative:
  files:
    contracts/Token.sol: |
      uint256 deadline = block.timestamp + 300;
positive:
  files:
    contracts/Token.sol: |
      require(block.timestamp % 2 == 0);
  expect: WRONG
