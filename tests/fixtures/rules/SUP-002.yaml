rule: SUP-002
negative:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        build:
          steps:
            - run: npm ci
positive:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        build:
          steps:
            - run: echo hello
  expect: MISSING
