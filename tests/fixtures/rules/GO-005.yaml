rule: GO-005
negative:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        lint:
          steps:
            - run: go vet ./...
positive:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        build:
          steps:
            - run: go build ./...
  expect: MISSING
