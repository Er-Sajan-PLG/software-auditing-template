rule: SUP-008
negative:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        build:
          steps:
            - run: echo hello
positive:
  files:
    .github/workflows/ci.yml: |
      name: CI
      jobs:
        build:
          steps:
            - run: echo ${{ github.event.pull_request.title }}
  expect: FAIL
